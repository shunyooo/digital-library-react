version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
        - ./app:/usr/src/app:delegated
    ports:
        - 3000:3000
    command: sh -c "npm start"
    stdin_open: true
    env_file: .env
